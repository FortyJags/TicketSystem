<Window x:Class="TicketSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicketSystem"
        mc:Ignorable="d"
        Title="Ticket Master" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Name="Order" Text="{Binding OrderAsList}"/>

        <Button Height="100" Width="100" Content="Generate Ticket" Click="GenearateTicket_Click" Grid.Column="2" Grid.Row="2"/>
        <Button  Height="100" Width="100" HorizontalAlignment="Left" Content="Burger" Click="Additem_Click" Grid.Column="1" />
        <Button  Height="100" Width="100" HorizontalAlignment="Right" Content="Cake" Click="Additem_Click" Grid.Column="1"/>
    </Grid>
</Window>
